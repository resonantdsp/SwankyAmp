# Reproducible source and bundle metadata on every build platform.
* text=auto eol=lf

# These files are a byte-for-byte extraction used as the released-tone oracle.
verification/reference/released/** -text

# Generated from the released bank byte for byte, CRLF line ends included.
presets/*.xml -text
